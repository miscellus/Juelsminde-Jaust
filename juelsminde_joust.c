#include "juelsminde_joust.h"

#define FONT_SPACING_FOR_SIZE 0.12f

#define MINIMUM(a, b) ((a) < (b) ? (a) : (b))
#define MAXIMUM(a, b) ((a) > (b) ? (a) : (b))

void
jj_update_and_render(JJ_Input input, JJ_Render_Context render) {
	(void)input;
	(void)render;
}